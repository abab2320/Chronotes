<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chronotes.backend.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.chronotes.backend.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="username" column="username"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="bio" column="bio"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据邮箱查询用户 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password, username, avatar_url, bio, status, create_time, update_time
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 根据ID查询用户 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password, username, avatar_url, bio, status, create_time, update_time
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.chronotes.backend.entity.User" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (email, password, username, avatar_url, bio, status, create_time, update_time)
        VALUES (#{email}, #{password}, #{username}, #{avatarUrl}, #{bio}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.chronotes.backend.entity.User">
        UPDATE user
        <set>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="username != null">username = #{username},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

</mapper>
