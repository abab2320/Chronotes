# Chronotes 组件使用示例

展示如何使用 Chronotes 设计系统中的各个组件。

## 📦 快速开始

### 1. 引入设计令牌

```typescript
// main.ts
import './styles/design-tokens.css'
```

### 2. 注册组件（可选）

```typescript
// 全局注册
import ChButton from '@/components/ChButton.vue'
import ChCard from '@/components/ChCard.vue'
import ChInput from '@/components/ChInput.vue'
import ChTag from '@/components/ChTag.vue'
import ChModal from '@/components/ChModal.vue'

app.component('ChButton', ChButton)
app.component('ChCard', ChCard)
// ... 其他组件
```

---

## 🔘 ChButton - 按钮组件

### 基础用法

```vue
<template>
  <div class="demo-buttons">
    <ChButton type="primary">主要按钮</ChButton>
    <ChButton type="secondary">次要按钮</ChButton>
    <ChButton type="text">文本按钮</ChButton>
    <ChButton type="danger">危险按钮</ChButton>
  </div>
</template>
```

### 不同尺寸

```vue
<template>
  <ChButton size="sm">小按钮</ChButton>
  <ChButton size="md">中按钮</ChButton>
  <ChButton size="lg">大按钮</ChButton>
</template>
```

### 带图标

```vue
<template>
  <ChButton icon="✓">确认</ChButton>
  <ChButton icon="✕" type="danger">删除</ChButton>
  <ChButton icon="📝">编辑</ChButton>
</template>
```

### 加载状态

```vue
<script setup>
import { ref } from 'vue'
const loading = ref(false)

const handleClick = () => {
  loading.value = true
  setTimeout(() => {
    loading.value = false
  }, 2000)
}
</script>

<template>
  <ChButton :loading="loading" @click="handleClick">
    {{ loading ? '加载中...' : '点击加载' }}
  </ChButton>
</template>
```

### 圆形按钮

```vue
<template>
  <ChButton circle icon="✓" type="primary" />
  <ChButton circle icon="✕" type="danger" />
  <ChButton circle icon="⚙" type="secondary" />
</template>
```

---

## 🎴 ChCard - 卡片组件

### 基础用法

```vue
<template>
  <ChCard>
    <p>这是卡片内容</p>
  </ChCard>
</template>
```

### 带标题和底部

```vue
<template>
  <ChCard>
    <template #header>
      <div style="display: flex; justify-content: space-between; align-items: center;">
        <h3>卡片标题</h3>
        <ChTag>新</ChTag>
      </div>
    </template>
    
    <p>这是卡片的主要内容区域...</p>
    
    <template #footer>
      <span class="date">📅 2025-11-26</span>
      <ChButton size="sm" type="primary">查看详情</ChButton>
    </template>
  </ChCard>
</template>
```

### 笔记卡片示例

```vue
<template>
  <ChCard hoverable>
    <template #header>
      <h3 style="font-size: 16px; font-weight: 600;">Vue 3 学习笔记</h3>
    </template>
    
    <div style="display: flex; gap: 8px; margin-bottom: 12px;">
      <ChTag color="primary">Vue</ChTag>
      <ChTag color="secondary">前端</ChTag>
    </div>
    
    <p style="color: var(--color-text-secondary); font-size: 14px; line-height: 1.6;">
      深入学习 Vue 3 的 Composition API，包括 ref、reactive、computed 等核心概念...
    </p>
    
    <template #footer>
      <span style="font-size: 12px; color: var(--color-text-tertiary);">📅 2025-11-23</span>
      <div style="display: flex; gap: 4px;">
        <ChButton size="sm" type="text" icon="✏️">编辑</ChButton>
        <ChButton size="sm" type="text" icon="🗑️">删除</ChButton>
      </div>
    </template>
  </ChCard>
</template>
```

### 自定义圆角和内边距

```vue
<template>
  <ChCard radius="lg" padding="lg" :shadow="false">
    <p>自定义样式的卡片</p>
  </ChCard>
</template>
```

---

## 📝 ChInput - 输入框组件

### 基础用法

```vue
<script setup>
import { ref } from 'vue'
const value = ref('')
</script>

<template>
  <ChInput v-model="value" placeholder="请输入内容..." />
</template>
```

### 不同尺寸

```vue
<template>
  <ChInput v-model="value" size="sm" placeholder="小尺寸" />
  <ChInput v-model="value" size="md" placeholder="中尺寸" />
  <ChInput v-model="value" size="lg" placeholder="大尺寸" />
</template>
```

### 带图标

```vue
<template>
  <ChInput 
    v-model="searchValue"
    icon="🔍"
    placeholder="搜索笔记、任务..."
  />
  
  <ChInput 
    v-model="email"
    icon="📧"
    suffix-icon="✓"
    placeholder="邮箱地址"
  />
</template>
```

### 可清空

```vue
<template>
  <ChInput 
    v-model="value"
    clearable
    placeholder="可清空的输入框"
    @clear="handleClear"
  />
</template>
```

### 密码输入

```vue
<template>
  <ChInput 
    v-model="password"
    type="password"
    icon="🔒"
    placeholder="请输入密码"
  />
</template>
```

### 禁用和只读

```vue
<template>
  <ChInput v-model="value" disabled placeholder="禁用状态" />
  <ChInput v-model="value" readonly placeholder="只读状态" />
</template>
```

---

## 🏷️ ChTag - 标签组件

### 基础用法

```vue
<template>
  <ChTag>默认标签</ChTag>
  <ChTag color="primary">主色</ChTag>
  <ChTag color="secondary">副色</ChTag>
  <ChTag color="success">成功</ChTag>
  <ChTag color="warning">警告</ChTag>
  <ChTag color="error">错误</ChTag>
</template>
```

### 不同尺寸

```vue
<template>
  <ChTag size="sm">小标签</ChTag>
  <ChTag size="md">中标签</ChTag>
  <ChTag size="lg">大标签</ChTag>
</template>
```

### 可关闭

```vue
<script setup>
import { ref } from 'vue'
const tags = ref(['Vue', 'TypeScript', '前端', '学习'])

const removeTag = (index: number) => {
  tags.value.splice(index, 1)
}
</script>

<template>
  <ChTag
    v-for="(tag, index) in tags"
    :key="tag"
    closable
    @close="removeTag(index)"
  >
    {{ tag }}
  </ChTag>
</template>
```

### 标签云示例

```vue
<template>
  <div style="display: flex; flex-wrap: wrap; gap: 8px;">
    <ChTag color="primary">Vue</ChTag>
    <ChTag color="secondary">TypeScript</ChTag>
    <ChTag color="success">算法</ChTag>
    <ChTag color="info">学习</ChTag>
    <ChTag color="warning">前端</ChTag>
    <ChTag>管理</ChTag>
  </div>
</template>
```

---

## 🪟 ChModal - 模态框组件

### 基础用法

```vue
<script setup>
import { ref } from 'vue'
const visible = ref(false)

const openModal = () => {
  visible.value = true
}

const handleConfirm = () => {
  // 处理确认逻辑
  visible.value = false
}
</script>

<template>
  <ChButton @click="openModal">打开模态框</ChButton>
  
  <ChModal v-model="visible" title="提示">
    <p>这是模态框的内容</p>
    <template #footer>
      <ChButton type="secondary" @click="visible = false">取消</ChButton>
      <ChButton type="primary" @click="handleConfirm">确定</ChButton>
    </template>
  </ChModal>
</template>
```

### 自定义宽度

```vue
<template>
  <ChModal v-model="visible" title="宽屏模态框" width="800px">
    <p>更宽的内容区域...</p>
  </ChModal>
</template>
```

### 自定义标题

```vue
<template>
  <ChModal v-model="visible">
    <template #title>
      <div style="display: flex; align-items: center; gap: 8px;">
        <span>📝</span>
        <h3>新建笔记</h3>
      </div>
    </template>
    
    <ChInput v-model="noteTitle" placeholder="笔记标题" />
    <!-- 更多表单内容... -->
    
    <template #footer>
      <ChButton type="secondary" @click="visible = false">取消</ChButton>
      <ChButton type="primary" @click="createNote">创建</ChButton>
    </template>
  </ChModal>
</template>
```

### 完整的表单示例

```vue
<script setup>
import { ref } from 'vue'

const showModal = ref(false)
const formData = ref({
  title: '',
  tags: [],
  content: ''
})

const handleSubmit = () => {
  console.log('提交数据:', formData.value)
  showModal.value = false
}
</script>

<template>
  <ChButton icon="+" @click="showModal = true">新建笔记</ChButton>
  
  <ChModal 
    v-model="showModal" 
    title="新建笔记" 
    width="600px"
    :close-on-click-modal="false"
  >
    <div style="display: flex; flex-direction: column; gap: 16px;">
      <div>
        <label style="display: block; margin-bottom: 8px; font-weight: 500;">标题</label>
        <ChInput 
          v-model="formData.title" 
          placeholder="输入笔记标题..."
          size="lg"
        />
      </div>
      
      <div>
        <label style="display: block; margin-bottom: 8px; font-weight: 500;">标签</label>
        <div style="display: flex; gap: 8px;">
          <ChTag closable>Vue</ChTag>
          <ChTag closable>学习</ChTag>
          <ChButton size="sm" type="text" icon="+">添加标签</ChButton>
        </div>
      </div>
      
      <div>
        <label style="display: block; margin-bottom: 8px; font-weight: 500;">内容</label>
        <textarea 
          v-model="formData.content"
          placeholder="开始记录..."
          style="
            width: 100%;
            min-height: 200px;
            padding: 12px;
            border: 1px solid var(--color-border);
            border-radius: var(--radius-lg);
            font-family: inherit;
            resize: vertical;
          "
        />
      </div>
    </div>
    
    <template #footer>
      <ChButton type="secondary" @click="showModal = false">取消</ChButton>
      <ChButton type="primary" icon="✓" @click="handleSubmit">创建笔记</ChButton>
    </template>
  </ChModal>
</template>
```

---

## 🎨 综合示例 - 笔记列表

```vue
<script setup lang="ts">
import { ref } from 'vue'

const notes = ref([
  {
    id: 1,
    title: 'Vue 3 Composition API',
    tags: ['Vue', '前端'],
    summary: '深入学习 Vue 3 的核心概念...',
    date: '2025-11-23'
  },
  {
    id: 2,
    title: 'TypeScript 高级类型',
    tags: ['TypeScript', '编程'],
    summary: '总结 TypeScript 高级特性...',
    date: '2025-11-22'
  }
])

const searchValue = ref('')
const showModal = ref(false)
</script>

<template>
  <div style="padding: 32px;">
    <!-- 顶部操作栏 -->
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
      <h2 style="font-size: 28px; font-weight: 600; color: var(--color-text-primary);">
        我的笔记
      </h2>
      <div style="display: flex; gap: 12px;">
        <ChInput 
          v-model="searchValue"
          icon="🔍"
          placeholder="搜索笔记..."
          style="width: 300px;"
        />
        <ChButton type="primary" icon="+" @click="showModal = true">
          新建笔记
        </ChButton>
      </div>
    </div>
    
    <!-- 笔记网格 -->
    <div style="
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 20px;
    ">
      <ChCard 
        v-for="note in notes"
        :key="note.id"
        hoverable
      >
        <template #header>
          <div style="display: flex; justify-content: space-between; align-items: flex-start;">
            <h3 style="font-size: 16px; font-weight: 600; flex: 1;">
              {{ note.title }}
            </h3>
            <div style="display: flex; gap: 4px; opacity: 0.7;">
              <ChButton circle size="sm" type="text" icon="✏️" />
              <ChButton circle size="sm" type="text" icon="🗑️" />
            </div>
          </div>
        </template>
        
        <div style="display: flex; gap: 8px; margin-bottom: 12px; flex-wrap: wrap;">
          <ChTag 
            v-for="tag in note.tags" 
            :key="tag"
            color="secondary"
            size="sm"
          >
            {{ tag }}
          </ChTag>
        </div>
        
        <p style="
          font-size: 14px;
          color: var(--color-text-secondary);
          line-height: 1.6;
          margin-bottom: 12px;
        ">
          {{ note.summary }}
        </p>
        
        <template #footer>
          <span style="font-size: 12px; color: var(--color-text-tertiary);">
            📅 {{ note.date }}
          </span>
          <ChButton size="sm" type="primary">查看详情</ChButton>
        </template>
      </ChCard>
    </div>
    
    <!-- 新建笔记模态框 -->
    <ChModal v-model="showModal" title="新建笔记" width="600px">
      <div style="display: flex; flex-direction: column; gap: 16px;">
        <ChInput placeholder="笔记标题..." size="lg" />
        <ChInput placeholder="添加标签..." icon="🏷️" />
      </div>
      
      <template #footer>
        <ChButton type="secondary" @click="showModal = false">取消</ChButton>
        <ChButton type="primary" icon="✓">创建</ChButton>
      </template>
    </ChModal>
  </div>
</template>
```

---

## 📱 响应式布局示例

```vue
<template>
  <div class="responsive-layout">
    <div class="notes-grid">
      <ChCard v-for="i in 6" :key="i" hoverable>
        <template #header>
          <h3>笔记 {{ i }}</h3>
        </template>
        <p>笔记内容...</p>
      </ChCard>
    </div>
  </div>
</template>

<style scoped>
.notes-grid {
  display: grid;
  gap: 20px;
  
  /* 默认：单列 */
  grid-template-columns: 1fr;
}

/* 平板：两列 */
@media (min-width: 768px) {
  .notes-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* 桌面：三列 */
@media (min-width: 1024px) {
  .notes-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* 大屏：四列 */
@media (min-width: 1536px) {
  .notes-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}
</style>
```

---

## 🎯 最佳实践

### 1. 组件组合

```vue
<!-- 好的做法：组合使用组件 -->
<ChCard>
  <template #header>
    <div class="card-title">
      <h3>标题</h3>
      <ChTag color="success">完成</ChTag>
    </div>
  </template>
  <p>内容...</p>
  <template #footer>
    <ChButton type="primary">操作</ChButton>
  </template>
</ChCard>
```

### 2. 响应式设计

```vue
<!-- 移动端适配 -->
<ChInput 
  :size="isMobile ? 'sm' : 'md'"
  placeholder="搜索..."
/>
```

### 3. 性能优化

```vue
<!-- 使用 v-show 而非 v-if 处理频繁切换的模态框 -->
<ChModal v-model="visible">
  <!-- 内容... -->
</ChModal>
```

---

**文档版本**：v1.0  
**最后更新**：2025-11-26  
**适用项目**：Chronotes Frontend
