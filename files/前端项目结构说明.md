
# 📘 Chronotes 前端项目结构规范文档（v1.0）

> **适用范围：**Chronotes Web 前端项目
> **技术栈：**Vue 3、Vite、TypeScript、Pinia、Vue Router、Element Plus、VeeValidate
> **文件规范目的：**确保团队协作统一、代码结构清晰、可扩展性强。

---

# 1. 项目目录总览

```
chronotes-frontend/
│
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   ├── pages/
│   ├── router/
│   ├── store/
│   ├── styles/
│   ├── utils/
│   ├── App.vue
│   └── main.ts
│
├── index.html
└── package.json
```

---

# 2. 各目录职责说明

## 2.1 `api/` —— 后端接口封装（统一 axios 管理）

**作用：**统一管理所有后端 API 调用，所有请求必须通过此目录封装。

目录示例：

```
api/
├── axiosInstance.ts   # axios 实例，拦截器、TOKEN、BASE_URL
├── auth.ts            # 登录/注册/Token管理相关 API
├── tasks.ts           # 任务相关 API
└── notes.ts           # 笔记相关 API
```

**要求：**

* 必须使用 axiosInstance，禁止直接 new axios()
* 每个业务模块一个独立 API 文件
* API 函数全部使用 TypeScript 类型定义

---

## 2.2 `assets/` —— 静态资源（仅放不会被 JS 处理的文件）

```
assets/
├── images/
├── icons/
├── fonts/
└── styles/ (不放样式文件，样式放 styles/)
```

**注意：**

* 全局样式不放这里！
* 图片必须按模块分类，如 `tasks/`、`auth/`

---

## 2.3 `components/` —— 公共组件（可复用）

```
components/
├── AppButton.vue
├── AppCard.vue
├── AppInput.vue
└── ...
```

**规范：**

* 所有公共组件以 `AppXxx` 命名
* 不带业务逻辑，只负责 UI 表现
* 可复用组件必须与页面解耦

---

## 2.4 `layouts/` —— 布局组件（页面骨架）

```
layouts/
├── MainLayout.vue       # 已登录用户主界面（侧边栏 + 顶栏 + 内容）
└── AuthLayout.vue       # 登录注册界面（无侧边栏）
```

**用于控制全局结构，不放业务内容。**

---

## 2.5 `pages/` —— 路由级页面（业务页面）

```
pages/
├── Login/
│   ├── index.vue
│   └── components/
│
├── Register/
│   ├── index.vue
│   └── components/
│
├── Tasks/
│   ├── index.vue
│   └── components/
│
└── Notes/
    ├── index.vue
    └── components/
```

**规范：**

* 每个页面一个文件夹
* 页面私有组件放 `components/` 中
* 页面根组件统一命名为 `index.vue`

---

## 2.6 `router/` —— 路由配置

```
router/
└── index.ts
```

**要求：**

* 按页面划分路由
* 使用 `meta.requiresAuth` 控制登录权限
* 统一在 router.beforeEach 做权限验证

---

## 2.7 `store/` —— Pinia 状态管理（全局共享状态）

```
store/
├── user.ts       # 用户状态（token、用户信息）
└── app.ts        # 全局 UI 状态（主题、侧边栏开关）
```

**要求：**

* 每个模块一个 store 文件
* store 名统一小写：useUserStore、useAppStore

---

## 2.8 `styles/` —— 全局样式与变量文件

```
styles/
├── reset.scss         # 浏览器默认样式重置
├── variables.scss     # SCSS 全局变量（颜色、间距、阴影）
└── global.scss        # 全局通用样式
```

**要求：**

* 所有颜色、尺寸必须从 variables.scss 中取
* 严禁在组件中写硬编码颜色

---

## 2.9 `utils/` —— 工具库（纯函数）

```
utils/
├── validators.ts   # 表单验证
└── date.ts         # 时间格式处理
```

**规范：**

* 只写工具函数，不依赖组件
* 只使用纯函数（无副作用）
* 模块需按领域拆分

---

# 3. 项目文件规范

## 3.1 命名规范

| 类型   | 规则                         |
| ---- | -------------------------- |
| 文件夹  | 小写、多个单词用驼峰，如 `userProfile` |
| 公共组件 | `AppXxx.vue`               |
| 页面组件 | 文件夹名大写，根组件 `index.vue`     |
| 变量   | camelCase                  |
| 枚举   | PascalCase                 |
| 接口类型 | 以 `I` 开头，如：`IUser`         |

---

## 3.2 组件规范

### 必须遵守：

* 使用 `<script setup lang="ts">`
* 必须写类型（Props、接口、返回值）
* 样式使用 `scoped`

---

## 3.3 API 返回值规范

统一封装 API 返回值：

```ts
interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
}
```

---

# 4. 路由规范

* 使用路径与页面目录 **一一对应**
* 权限判断必须在 router.beforeEach 中实现
* 所有需要登录的路由都必须加：

  ```ts
  meta: { requiresAuth: true }
  ```

---

# 5. 团队协作要求

* **新增页面必须说明路由、状态管理变化**
* **公共组件必须提前沟通后再添加**
* **新增 API 必须同步后端接口文档**
* **禁止在页面内写大量逻辑，尽量抽到 utils 或 composables**

---

# 6. 未来扩展建议（v1.1）

后续可能加入：

* `composables/` —— 组合式函数
* `constants/` —— 全局常量定义
* `hooks/` —— 可复用逻辑块

---

# 📎 附录：完整目录模板（可直接给新成员复制）

```
src/
├── api/
├── assets/
├── components/
├── layouts/
├── pages/
├── router/
├── store/
├── styles/
├── utils/
├── App.vue
└── main.ts
```
